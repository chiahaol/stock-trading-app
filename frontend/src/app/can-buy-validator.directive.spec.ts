import { CanBuyValidatorDirective } from './can-buy-validator.directive';

describe('CanBuyValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CanBuyValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
